Create a conda environment using Python 3.8 and miniconda
Run from terminal: conda install libpython m2w64-toolchain -c msys2
Run from terminal: pip install -r requirements.txt
In the main configuration file, add the environment variable PYSPARK_PYTHON=python3